import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

export const PXCSlice = createSlice({
	name: 'PXC',
	initialState,
	reducers: {},
});

// export const {} = PXCSlice.actions;
export default PXCSlice.reducer;
