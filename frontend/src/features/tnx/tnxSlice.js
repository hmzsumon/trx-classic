import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

export const tnxSlice = createSlice({
	name: 'tnx',
	initialState,
	reducers: {},
});

// export const {} = tnxSlice.actions;
export default tnxSlice.reducer;
