import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

export const trxcSlice = createSlice({
	name: 'trxc',
	initialState,
	reducers: {},
});

// export const {} = PXCSlice.actions;
export default trxcSlice.reducer;
