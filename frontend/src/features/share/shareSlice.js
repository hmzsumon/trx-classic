import { createSlice } from '@reduxjs/toolkit';

const initialState = {};

export const shareSlice = createSlice({
	name: 'share',
	initialState,
	reducers: {},
});

// export const {} = PXCSlice.actions;
export default shareSlice.reducer;
